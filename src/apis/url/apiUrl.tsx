const api_version = "api/v1";
const ip = "/cors";

export const API_URLS = {
    adminLogin: `${ip}/${api_version}/admins/login/account`,
    questionBank:`${ip}/${api_version}/questionBank`,
    examQuestion:`${ip}/${api_version}/examQuestion`,
    users:`${ip}/${api_version}/users`,
    userLinkCategory:`${ip}/${api_version}/userLinkCategory`,
    study:`${ip}/${api_version}/study`,
    admin:`${ip}/${api_version}/admins`,
}